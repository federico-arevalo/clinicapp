import { FormatDNIPipe } from './format-dni.pipe';

describe('FormatDNIPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatDNIPipe();
    expect(pipe).toBeTruthy();
  });
});
