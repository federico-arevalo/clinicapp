import { TurnosColorDirective } from './turnos-color.directive';

describe('TurnosColorDirective', () => {
  it('should create an instance', () => {
    const directive = new TurnosColorDirective();
    expect(directive).toBeTruthy();
  });
});
